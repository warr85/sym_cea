{% extends 'base.html.twig' %}

{% block body %}
    <h1>OfertaAcademica</h1>

    <table>
        <tbody>
            <tr>
                <th>Aula</th>
                <td>{{ ofertaAcademica.aula }}</td>
            </tr>
            <tr>
                <th>Cupo</th>
                <td>{{ ofertaAcademica.cupo }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ ofertaAcademica.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ceapp_gestion_oferta_academica_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ceapp_gestion_oferta_academica_edit', { 'id': ofertaAcademica.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
