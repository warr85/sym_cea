{% extends 'base.html.twig' %}

{% block body %}
    <h1>PlanificacionCalificacion list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for planificacionCalificacion in planificacionCalificacions %}
            <tr>
                <td><a href="{{ path('ceapp_docente_calificar_show', { 'id': planificacionCalificacion.id }) }}">{{ planificacionCalificacion.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('ceapp_docente_calificar_show', { 'id': planificacionCalificacion.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('ceapp_docente_calificar_edit', { 'id': planificacionCalificacion.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ceapp_docente_calificar_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
